# Build directories. One tree per target: build-linux, build-win, build-arm64.
build/
build-*/

# Generated by the shader build step and by the miner at runtime.
*.spv
tune.json
pipeline-cache.bin

# Local configuration. Never commit a file holding pool credentials or a
# wallet address; copy config-template.json and edit the copy.
config.json

# Editor, tooling and OS noise.
compile_commands.json
.cache/
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
